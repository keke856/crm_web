LinK React Laravel:
 import:
   import { Link, Head } from '@inertiajs/react';
     <Link href={route('dashboard')}
        className="font-semibold text-gray-600 hover:text-gray-900 dark:text-gray-400 dark:hover:text-white focus:outline focus:outline-2 focus:rounded-sm focus:outline-red-500" >  Dashboard </Link>


Enviar POST

import { Head, Link, useForm } from '@inertiajs/react';
 post(route('login'));

    const { data, setData, post, processing, errors, reset } = useForm({
        email: '',
        password: '',
        remember: false,
    });




